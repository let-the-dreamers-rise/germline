.env
node_modules/
artifacts/
cache/
typechain-types/
.next/
out/
deployments/*.local.json
*.log

# Content-addressed caches. Reproducible from the chain plus the engine, so
# committing them would only add noise.
storage/
genomes/
